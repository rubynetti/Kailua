<%= form_for(soci) do |f| %>
  <% if soci.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(soci.errors.count, "error") %> prohibited this soci from being saved:</h2>

      <ul>
      <% soci.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_area :nome %>
  </div>

  <div class="field">
    <%= f.label :cognome %>
    <%= f.text_area :cognome %>
  </div>

  <div class="field">
    <%= f.label :contatto %>
    <%= f.text_area :contatto %>
  </div>

  <div class="field">
    <%= f.label :cf %>
    <%= f.text_area :cf %>
  </div>

  <div class="field">
    <%= f.label :numero_tessera %>
    <%= f.text_area :numero_tessera %>
  </div>

  <div class="field">
    <%= f.label :da_rinnovare %>
    <%= f.check_box :da_rinnovare %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
